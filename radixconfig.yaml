apiVersion: radix.equinor.com/v1
kind: RadixApplication
metadata:
  name: dash_app.webviz_app
spec:
  environments:
    - name: dev
      build:
        from: main
    - name: prod
  components:
    - name: main
      src: "."
      publicPort: http
      ports:
       - name: http
         port: 5000
